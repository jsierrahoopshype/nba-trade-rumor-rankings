name: scrape-and-commit

on:
  workflow_dispatch:
  # You can adjust this schedule if you want
  schedule:
    # Every hour at minute 5 (change as you like)
    - cron: "5 * * * *"

jobs:
  scrape-and-commit:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install huggingface_hub

      - name: Run scraper
        env:
          HH_PREVIEW_USER: ${{ secrets.HH_PREVIEW_USER }}
          HH_PREVIEW_PASS: ${{ secrets.HH_PREVIEW_PASS }}
        run: python scrape_trade_rumors.py

      # 🔧 NEW STEP: fix player names after scraping
      - name: Fix player names in CSV
        run: python fix_trade_rumors_players.py

      - name: Configure git user
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Commit and push changes (if any)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git status
          if git diff --quiet; then
            echo "No changes to commit."
          else
            git add trade_rumors.csv
            git commit -m "Update trade rumors CSV"
            git push
          fi
